package fishmaple.utils;

import fishmaple.Objects.FileObject;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

public class mainTest {
   /* public static void main(String args[]){
        File file=new File("D:/WebStorm 2018.2");


    }



    public static Long getBig(File file,Long q){
        File [] files = file.listFiles();//获取包含file对象对应的子目录或者文件
        for(int i =0;i<files.length;i++){
            if(files[i].isFile()){//判断是否为文件
                q+=files[i].length();
            }else{
                getBig(files[i],q);//否则重新递归到方法中
            }
        }
        q+=file.length();
        return q;
    }*/
}
